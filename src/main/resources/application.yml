spring:
  application:
    name: fraud-detection-service

  datasource:
    url: jdbc:postgresql://localhost:5432/frauddb
    username: frauduser
    password: fraudpass

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false

  data:
    redis:
      host: localhost
      port: 6379

  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8081

# ======================================================
# 🔐 JWT CONFIGURATION
# ======================================================
security:
  jwt:
    secret: "c0a96af3e20a47fb987b1d4a4b5fd96c1c4a44b232650bd8bb4e381b573d56e3"
    expiration: 3600000   # 1 hora en milisegundos

# ======================================================
# 👤 SUPER ADMIN DEFAULT (SEED)
# (ahora mismo no lo usamos directamente, pero está listo
#  por si luego quieres leerlo desde un @ConfigurationProperties)
# ======================================================
app:
  admin:
    username: "admin"
    password: "admin123"
    role: "ADMIN"

# ======================================================
# 📊 SPRING BOOT ACTUATOR
# ======================================================
management:
  endpoints:
    web:
      exposure:
        include: "health,info,metrics,httpexchanges"
  endpoint:
    health:
      show-details: when_authorized   # detalles solo si estás autenticada
  info:
    env:
      enabled: true
